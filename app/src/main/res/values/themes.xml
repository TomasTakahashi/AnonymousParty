<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AnonymousParty" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/colorPrimaryLight</item>
        <item name="colorSecondary">@color/colorSecondaryLight</item>
        <item name="background">@color/BackgroundLight</item>
        <item name="backgroundColor">@color/colorPrimaryLight</item>
        <item name="android:windowBackground">@color/BackgroundLight</item>
        <item name="colorAccent">@color/contrastLight</item>
        <item name="android:textColor">@color/contrastLight</item>
        <item name="tint">@color/contrastLight</item>
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <!-- <style name="Theme.AnonymousParty" parent="Theme.AnonymousParty" /> -->
</resources>